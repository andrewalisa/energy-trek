<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chase.EnergyTrek">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar"
        android:background="@drawable/space">

        <activity

            android:name=".MainActivity"
            android:label="@string/app_name"
            android:background="@drawable/space">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.

     taken out due to new thing trying
     <activity android:name=".MainActivity"
            android:label="@string/app_name">

        </activity>

     add back in once database is done.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:name=".HealthInfoActivity"
            android:label="Add Data Point">
        </activity>

        <activity
            android:name=".GoalsActivity"
            android:label="Today">
        </activity>

        <activity
            android:name=".PreviousTreksActivity"
            android:label="Previous Treks">
        </activity>


    </application>

</manifest>