<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chase.EnergyTrek">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">

            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"/>

                <category
                    android:name="android.intent.category.LAUNCHER"/>

            </intent-filter>
        </activity>

        <!--
 ATTENTION: This was auto-generated to add Google Play services to your project for
     App Indexing.  See https://g.co/AppIndexing/AndroidStudio for more information.
        -->
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity
            android:label="@string/healthInfoActivityTitle"
            android:name=".HealthInfoActivity">
        </activity>

        <activity
            android:label="@string/button_goals"
            android:name=".GoalsActivity">
        </activity>

        <activity
            android:label="@string/button_progress"
            android:name=".ProgressActivity">
        </activity>

        <activity
            android:label="@string/button_comparison"
            android:name=".ComparisonActivity">
        </activity>

    </application>
</manifest>